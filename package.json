{
  "name": "ngrp-brierley-portal-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && npm install && npm run build && npm start\" \"cd frontend && npm install && npm run build && npm start\"",
    "start:backend": "cd backend && npm run start:dev",
    "start:frontend": "cd frontend && npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
